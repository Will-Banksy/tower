// use crate::parser_new::scanner::Scanner;

// fn tokenise(code: &str) {
// 	let chars: Vec<char> = code.chars().collect();

// 	let scanner = Scanner::new(&chars);
// }

// fn tokenise_keyword(scanner: &mut Scanner<char>) {
// }

// fn tokenise_identifier(scanner: &mut Scanner<char>) {
// }